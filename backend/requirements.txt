fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
websockets==14.1
python-multipart==0.0.20
httpx==0.28.1
pytest==8.3.4
pytest-asyncio==0.25.1
sqlalchemy==2.0.36
psycopg[binary]==3.2.3

# D5a: optional at runtime; CSV export remains dependency-free.
gspread==6.1.4
google-auth==2.35.0

# --- Phase 4b: TTM (Granite TinyTimeMixer) stockout-risk detector ---
# Pulls in torch + transformers — a real, multi-minute install. Model weights
# (ibm-granite/granite-timeseries-ttm-r2) are expected pre-cached in the HF hub
# cache (see README) — this only installs the code to load and run them.
granite-tsfm[notebooks] @ git+https://github.com/ibm-granite/granite-tsfm.git
