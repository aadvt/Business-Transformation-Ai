import PageHeader, { EmptyState } from "@/components/PageHeader";

export default function CallPage() {
  return (
    <div>
      <PageHeader title="Call" subtitle="Live negotiation call view." />
      <EmptyState>Built in D5.</EmptyState>
    </div>
  );
}
